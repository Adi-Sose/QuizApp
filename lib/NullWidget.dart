import 'package:flutter/widgets.dart';

// A simple check if the passed object is null
bool notNull(Object obj) => obj != null;

// A widget that takes no space on the screen
Widget NullWidget () => Container(width: 0, height: 0);